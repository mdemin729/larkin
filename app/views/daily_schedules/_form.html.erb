<%= bootstrap_form_for(@daily_schedule, layout: :horizontal, label_col: 'col-sm-2', control_col: 'col-sm-10') do |f| %>
    <% if @daily_schedule.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@daily_schedule.errors.count, "error") %> prohibited this daily_schedule from being
            saved:</h2>

          <ul>
            <% @daily_schedule.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.date_select :delivery_date %>
    <%= f.submit %>
<% end %>
